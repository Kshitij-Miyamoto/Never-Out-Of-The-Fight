#include<iostream>
using namespace std ; 
int main()
{
    int arr1[] = {1, 2, 3, 1, 2, 4, 1} ; 
    int arr2[] = {2, 1, 3, 1, 5, 2, 2} ; 
    int  n = sizeof(arr1)/sizeof(arr1[0]);
    for(int i = 0 ; i < n ; i++){
        for( int j = 0; j < n ; j++){
            if(arr1[i] == arr2[j]){
                
            }

        }
    }
return 0; 
}